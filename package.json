{
    "name": "@asimojs/router",
    "version": "0.0.1",
    "description": "Asimo router: JS router for progressive apps",
    "author": "Bertrand Laporte",
    "license": "MIT",
    "keywords": [
        "router",
        "progressive app",
        "dynamic load"
    ],
    "scripts": {
        "clean": "rimraf dist lib",
        "build": "yarn clean && tsc && vite build",
        "build-all": "yarn build && vitest run",
        "test": "vitest",
        "coverage": "vitest run --coverage",
        "dev": "vite",
        "serve": "vite preview"
    },
    "dependencies": {

    },
    "devDependencies": {
        "@vitest/coverage-v8": "^0.32.0",
        "rimraf": "^5.0.1",
        "typescript": "^4.9.4",
        "vite": "^4.3.9",
        "vitest": "^0.32.0"
    }
}
